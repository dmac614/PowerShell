<#

# psuedocode

Define your goals/tasks for the day.
Check if the monthly CSV exists.
Create or load the file.
Add a new record for today.
Save the updated data.

add: file versioning -- Save backups or log changes to prevent overwriting data.

## tracking a date ##

whatever date is being tracked, i must verify if a yearly folder exists for that year and i must verify if a monthly folder exists for that month
-- Then I can create the file in the appropriate folder

if the yearly folder doesnt exist, create it
if the monthly folder doesnt exist, create it


string prompt: are you tracking for the current or a previous day?
-- validateset(current,previous); answer stored in variable

if current day: (get-date).ToLongDateString() 
    - test the folderYear path exists; if not create it 
        - if it exists, test folderMonth path exists; if not create it
            - if the file doesnt exist, create it
            - if it exists, write-warning
                - use the current code to test if the file exists as that works for the current day 


elseif previous day: 
- [datetime]prompt which date do you want to track?
- enter a ToLongDateString i.e 01 December 2025
    - verify the year being matched: if it matches the current year
    - does a yearly folder exist for this date? if not, create it
        - verify the month being matched: if it matches the current month
        - does a monthly folder exist for this date? if not, create it 
            - test the file is not in this path already
            - create an out variable for the path being tracked
            - create the file in this path 
        - if the month does not match the current month
        - does a monthly folder exist for this month? if not, create it 
            - test the file is not in this path already
            - create an out variable for the path being tracked
            - create the file in this path 
    - if the year does not match the current year 
    - does a yearly folder exist for this year? if not, create it    
        - verify the month being matched: if it matches the current month
        - does a monthly folder exist for this date? if not, create it 
            - test the file is not in this path already
            - create an out variable for the path being tracked
            - create the file in this path 
        - if the month does not match the current month
        - does a monthly folder exist for this month? if not, create it 
            - test the file is not in this path already
            - create an out variable for the path being tracked
            - create the file in this path 


the month is feb
tracking the month of jan







try {
    specifyDate notin $Path\$folderName
    create a new file for this date
    [datetime]prompt + "_Tracker.csv"
}
catch {$error[0]} 