<#

# psuedocode

Define your goals/tasks for the day.
Check if the monthly CSV exists.
Create or load the file.
Add a new record for today.
Save the updated data.

add: file versioning -- Save backups or log changes to prevent overwriting data.


USER INPUT 

pscustomobject has empty values
the user is asked a question
- did you complete all tasks? If A = complete all tasks with Y
- if no; ask for answer for each task
Ask if any notes are to be added 
Append the answers to the pscustomobject



FOR each task in taskList DO
    DISPLAY task
    PROMPT user for completion status (Yes/No)
    PROMPT user for notes (optional)
        did you complete all tasks? If yes mark completed as yes
        if no, ask the user one by one for their answer

    CREATE dailyRecord as object {
        Date = today’s date
        Task = current task
        Completed = user input
        Notes = user input
    }

    APPEND dailyRecord to fileName
END FOR

DISPLAY message "Today's updates saved to " + fileName

END


##########################################################


gym day object -- complete
normal day object -- complete
current month variable 

# complete :)
Get the gym day dates of the current month
- Gym days: tuesday, friday, saturday
#


if its a gym day: N; 7K; 2L; G
else: N; 7K; 2L

Ask the user if they completed all the daily variables
- if not all, which variables were completed
- else, A = all

## possible addition ##
if a full day is completed, highligh the calendar day green





### Logical steps

navigate to folder path
-- does folder exist?
open the file
-- does file exist?
update values with Y or N
-- append data to the file
save file